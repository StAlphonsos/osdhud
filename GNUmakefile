# -*- makefile -*-
##
# GNU-make variant of osdhud makefile
##

PACKAGE_NAME=$(shell cat PACKAGE)
DIST_VERS=$(shell cat VERSION)
S=$(shell pwd)

include $(S)/config.mk
include $(S)/gnu/settings.mk
include $(S)/gnu/commands.mk
include $(S)/_makefile
